{
  "name": "chatgpt-project-root",
  "version": "1.0.0",
  "description": "Root package for Render deployment",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "npm install && cd frontend && npm install && npm run build && cd ../backend && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  }
}
